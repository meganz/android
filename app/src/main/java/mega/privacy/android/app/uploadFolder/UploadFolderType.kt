package mega.privacy.android.app.uploadFolder

/**
 * Enum class to define the type of the upload folder
 */
enum class UploadFolderType {
    /**
     * Select a folder
     */
    SINGLE_SELECT,

    /**
     * Select and upload a folder
     */
    SELECT_AND_UPLOAD
}