package mega.privacy.android.app.presentation.videoplayer.model

/**
 * Represents the type of player error encountered during video playback.
 */
enum class PlayerErrorType {
    FILE_NOT_SUPPORTED,
    CANNOT_PLAY,
    NO_NETWORK,
    VIDEO_NOT_RENDERED,
}
