<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/homepage"
    app:startDestination="@id/homepageFragment">
    <fragment
        android:id="@+id/homepageFragment"
        android:name="mega.privacy.android.app.lollipop.managerSections.HomepageFragment"
        android:label="fragment_homepage"
        tools:layout="@layout/fragment_homepage">
        <action
            android:id="@+id/action_homepageFragment_to_photosFragment"
            app:destination="@id/photosFragment" />
        <action
            android:id="@+id/action_homepage_to_fullscreen_offline"
            app:destination="@id/fullscreen_offline" />
        <action
            android:id="@+id/action_homepage_to_offline_file_info"
            app:destination="@id/offline_file_info"
            />
        <action
            android:id="@+id/action_homepage_to_recentBucket"
            app:destination="@id/recentBucketFragment"/>
    </fragment>
    <fragment
        android:id="@+id/photosFragment"
        android:name="mega.privacy.android.app.fragments.photos.PhotosFragment"
        android:label="@string/category_photos" />
    <fragment
        android:id="@+id/recentBucketFragment"
        android:name="mega.privacy.android.app.fragments.recent.RecentsBucketFragment">
        <argument
            android:name="serializedNodes"
            app:argType="string[]" />
        <argument
            android:name="bucket"
            app:argType="mega.privacy.android.app.BucketSaved" />
    </fragment>
    <fragment
        android:id="@+id/fullscreen_offline"
        android:name="mega.privacy.android.app.fragments.offline.OfflineFragment"
        tools:layout="@layout/fragment_offline">
        <argument
            android:name="path"
            android:defaultValue="/"
            app:argType="string" />
        <argument
            android:name="rootFolderOnly"
            android:defaultValue="true"
            app:argType="boolean" />
        <action
            android:id="@+id/action_homepage_to_fullscreen_offline"
            app:destination="@id/fullscreen_offline" />
        <action
            android:id="@+id/action_homepage_to_offline_file_info"
            app:destination="@id/offline_file_info"
            />
    </fragment>
    <fragment
        android:id="@+id/offline_file_info"
        android:name="mega.privacy.android.app.fragments.offline.OfflineFileInfoFragment"
        tools:layout="@layout/fragment_offline_file_info"
        >
        <argument
            android:name="handle"
            app:argType="string"
            />
    </fragment>
</navigation>
