package mega.privacy.android.domain.exception

/**
 * Exception thrown when an operation fails because the network is unavailable.
 */
class NetworkUnavailableException : RuntimeException("Network unavailable")
