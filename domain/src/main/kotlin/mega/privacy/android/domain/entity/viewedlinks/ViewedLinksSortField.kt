package mega.privacy.android.domain.entity.viewedlinks

/**
 * Sort fields available for the viewed-links list.
 */
enum class ViewedLinksSortField {
    /** Sort by file name. */
    Name,

    /** Sort by last accessed timestamp. */
    LastAccessed,
}
