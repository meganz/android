package mega.privacy.android.domain.entity.camerauploads

/**
 * Folder types for camera upload worker
 */
enum class CameraUploadFolderType {
    /**
     * Primary upload folder for camera uploads (photos and videos)
     */
    Primary,

    /**
     * Secondary upload folder for camera uploads (photos and videos)
     */
    Secondary
}
