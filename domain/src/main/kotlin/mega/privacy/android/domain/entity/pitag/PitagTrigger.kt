package mega.privacy.android.domain.entity.pitag

/**
 * Enum class defining each type of pitag trigger
 */
enum class PitagTrigger {
    NotApplicable,
    Picker,
    DragAndDrop,
    Camera,
    Scanner,
    SyncAlgorithm,
    ShareFromApp,
    CameraCapture,
    ExplorerExtension,
}