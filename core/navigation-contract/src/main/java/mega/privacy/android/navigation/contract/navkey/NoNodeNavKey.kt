package mega.privacy.android.navigation.contract.navkey

import androidx.navigation3.runtime.NavKey

/**
 * No node nav key
 *
 * Use this interface for all destinations that do not require nodes to be fetched
 */
interface NoNodeNavKey : NavKey